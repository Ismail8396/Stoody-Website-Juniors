package com.loam.stoody.global.constants;

public enum IndoorResponses{
    INIT,
    SUCCESS,
    FAIL,
    TOKEN_EXPIRED,
    TOKEN_ABSENT,
    USERNAME_EXIST,
    EMAIL_EXIST,
    USERNAME_EMAIL_EXIST
}
